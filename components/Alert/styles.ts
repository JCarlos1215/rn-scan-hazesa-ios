import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  dialog: {
    borderRadius: 20,
    backgroundColor: "white",
  },
  title: {
    textAlign: "center",
    fontWeight: "bold",
  },
  buttonLabel: {
    color: "green",
  },
});

